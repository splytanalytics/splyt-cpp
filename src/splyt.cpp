#include <stdio.h>

#include "splyt.h"

namespace splyt
{
    /** Initialize the Splyt SDK.

        This must be called first to use any
        functions provided in the SDK.
    */
    void Init() {
        printf("Splyt init.");
    }
}
